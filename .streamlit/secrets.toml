# Скопируй этот файл в .streamlit/secrets.toml (без .example на конце) и впиши СВОЙ ключ вместо плейсхолдера ниже.
# Файл .streamlit/secrets.toml НЕ должен попадать в git — он уже добавлен в .gitignore.

DEEPSEEK_API_KEY = "sk-впиши-сюда-свой-настоящий-ключ"
